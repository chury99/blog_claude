# 비밀값
.env

# 생성된 초안 (사람 검수 대기 — 로컬에만 보관)
drafts/*
!drafts/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# macOS
.DS_Store

# Claude Code 로컬 설정 (머신별)
.claude/settings.local.json

# 텔레그램 자격증명 (예시 파일만 커밋)
config/*.json
!config/*.json.example
