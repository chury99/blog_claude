# 인수인계 — 오늘의 특징주 브리핑

> 새 대화(Claude Code)가 이 프로젝트를 이어받기 위한 문서.
> 설계 배경은 [CONTEXT.md](CONTEXT.md), 사용법은 [README.md](README.md).
> 이 문서는 **무엇이 만들어졌고 무엇이 남았는지**만 담는다.

## 현재 상태 (2026-07-29)

**매일 자동 실행 중이다.** 제목에 '특징주'가 들어간 기사 수집 → 종목별로 묶어
이슈 3줄(무슨 일/세부·해석/예상 주가 방향 + 긍정·부정 판정) → 간밤 미국 증시 시황
→ HTML 리포트를 자체 웹서버 폴더에 저장 → 텔레그램 링크 발송.

- 저장소: https://github.com/chury99/blog_claude (Public, `main`).
- 서버: Mac Mini(이 머신). `claude` CLI 설치 완료(`~/.local/bin/claude`, v2.1.x).
  cron 무인 실행은 `config/claude.json` 의 장기 토큰(`claude setup-token`)으로 인증.
- 의존성: PyYAML, requests, holidays (Ghost·markdown 경로는 제거됨).

## 최근 변경 이력 (최신순)

- **2026-07-29 (3) — 발행 08시 → 07시, 기사 수집 구간 명시**.
  - crontab `0 7 * * * /Users/sh/blog_claude.sh` 로 변경 완료(다른 항목 보존 확인).
    `cron/blog_claude.sh` 주석·배포본도 갱신.
  - 수집 구간 = **직전 거래일 장 종료(`market_close`, 기본 15:30) ~ 실행 시각**.
    `common.last_close()` 가 계산하고 highlight·market·theme·feature 가 함께 쓴다
    (`stocknews.fetch` 는 `within_hours` → `since` 로 시그니처 변경).
  - ⚠️ **이 구간엔 특징주 기사가 거의 없다.** 1주일 실측(07-29): 국내 특징주 기사
    36건 중 16시~익일 07시 발행은 3건(8%)뿐. 07시 실행 재현에서 특징주 기반 종목 0개.
  - → **등락률 상위 종목 기사로 보강**하도록 `highlight.from_movers` 를 추가했다.
    상승·하락률 상위 40+40 종목을 훑어 구간 내 기사가 있는 종목을 채운다. 단
    '[마감시황]' 같은 시장 전체 기사가 종목에 딸려 오므로 **제목에 종목명이 든
    기사만** 인정한다. 07시 재현 결과 4종목 확보 → 리포트 정상 발행.
    설정은 `highlight.movers`(끄면 특징주 기사만 쓴다).
  - 기상 예약(`pmset`)은 필요 없다 — 이 Mac 은 절전에 들어가지 않게 설정돼 있다.

- **2026-07-29 (2) — 테마 기준 → '특징주' 기사 기준으로 재전환**. 인기 검색·테마
  등락률은 간접 지표라, 언론이 실제로 특징주 기사를 쓴 종목을 직접 신호로 삼았다.
  - `steps/highlight.py` + `prompts/highlight.txt` 신설.
  - **특징주 기사는 구글 뉴스 RSS 로 찾는다.** 네이버 금융 뉴스 섹션·mainnews(500건)·
    종목 뉴스 API 를 다 뒤졌지만 특징주 기사가 거의 없었다(종목뉴스 196건 중 1건).
    구글 뉴스는 검색 100건 중 25~30건이 특징주 기사다.
  - 구글 링크는 리다이렉트라 **본문을 못 받는다** → 네이버 종목 뉴스로 본문 근거를
    보탠다. 리포트엔 두 종류 링크가 함께 나가고 둘 다 요약 근거로 표시된다.
  - 종목명은 제목에서 뽑아 `ac.stock.naver.com` 자동완성으로 실제 종목인지 확인한다.
    시세는 `polling.finance.naver.com`.
  - `[특징주]`처럼 대괄호 안에 든 것만 인정 — '특징주 기사 선행매매 적발' 같은
    메타 기사가 섞이는 걸 막는다. 해외물은 `highlight.exclude_keywords` 로 제외.
  - 리포트 파일명이 `YYYYMMDD_오늘의특징주_브리핑.html` 로 바뀌었다.
  - 하루 claude 호출 = 종목 6 + 미국 시황 1 = **7회**.

- **2026-07-29 — 종목 기준 → 테마 기준으로 전면 전환**. 리포트 구조가
  [간밤 미국 증시] → [오늘 움직인 테마] → [테마별 핵심 이슈] 로 바뀌었다.
  - `steps/theme.py` + `prompts/theme.txt` 신설. 테마 목록·편입종목은 네이버 금융
    테마별 시세, 기사는 편입종목들의 뉴스를 모아 중복 제거.
  - `steps/stocknews.py` 신설 — 종목 뉴스 수집을 theme·feature 가 공유.
  - **편입 사유**(테마 상세 페이지 `p.info_txt`)를 프롬프트에 함께 넘긴다.
    단 "오늘 뉴스가 아님"을 명시해 과거 사유를 오늘 일처럼 쓰지 않게 했다.
  - 신규상장·SPAC·지주사는 `theme.exclude_keywords` 로 제외. 상장 형태별 묶음이라
    공통 재료가 없고, 요약하면 "개별 종목 이슈"라는 결론만 나와 호출만 낭비된다.
  - 리포트 파일명이 `YYYYMMDD_오늘의테마_브리핑.html` 로 바뀌었다.
  - 하루 claude 호출 = 테마 5 + 미국 시황 1 = **6회**.

- **2026-07-29 — 공시 기반 → 뉴스 기반 '오늘의 특징주'로 전환**. 리포트 구조가
  [간밤 미국 증시] → [인기 검색 종목 TOP 20] → [오늘의 특징주] 로 바뀌었다.
  - `steps/feature.py` + `prompts/feature.txt` 신설. 종목 뉴스는
    `api.stock.naver.com/news/stock/{code}`, 기사 링크는 원문으로 열린다.
  - `naver.py` 가 현재가·등락률까지 뽑는다. 레버리지·인버스 ETN/ETF 도 포함
    (인기 검색에 오르므로). 단 **파생상품은 종목 뉴스가 0건이라 특징주로는 안 잡힌다**
    — TOP 목록에는 등락률과 함께 나온다.
  - 공시 코드(`dart.py`·`brief.py`·`prompts/summarize.txt`)는 지우지 않고 남겼다.
    daily 에서만 뺐고 `pipeline.py dart` 로 확인 가능. 되살리려면 이 커밋 직전의
    `report.py` 렌더링(`_disc_card`)을 참고한다.
  - 리포트 파일명이 `YYYYMMDD_오늘의특징주_브리핑.html` 로 바뀌었다.

- **2026-07-28 — 리포트에 간밤 미국 증시 추가**. 리포트 맨 위에 다우존스·나스닥
  지수 카드와 흐름 2~3문장(`steps/market.py`, `prompts/us_market.txt`). 지수·뉴스
  모두 네이버 세계증시 API(인증키 없음), 요약은 claude 하루 1회. 실패해도 그
  부분만 빠지고 공시 브리핑은 나간다. 설정은 `config.yaml` 의 `market`.
  - **요약은 뉴스 기반**이다. claude 는 간밤 뉴스를 모르므로 지수 숫자만 주면 원인을
    못 쓴다. 네이버 금융 '해외증시' 기사(제목+발췌)를 함께 넘기고 원인은 그 안에서만
    쓰게 했다. 근거 기사는 제목·언론사와 함께 **원문 링크**를 리포트에 싣는다.
    기사가 없으면 요약을 건너뛰고 지수 숫자만 싣는다.
  - 세계증시 API 의 로이터 기사도 검토했으나 공개 퍼머링크가 없어 링크가 안 열린다.
    독자가 확인할 수 있어야 해서 링크되는 해외증시 목록 쪽을 골랐다.
  - 상위 3건은 **본문까지** 받아 넘긴다. 목록 발췌(100자)로는 "혼조 마감했음"
    수준밖에 안 나온다. 요약엔 지수 등락률을 다시 쓰지 않게 했다(위 카드와 중복).
  - 곁들여 **`claude -p` 를 프로젝트 밖 빈 폴더에서 실행**하도록 고쳤다. 프로젝트
    폴더에서 부르면 claude 가 저장소를 컨텍스트로 읽어 요약 대신 "저장소가 이런
    상태다" 같은 응답을 낼 때가 있었다(재현 확인). 기존 공시 요약에도 있던 위험.
  - `brief._tidy` → `common.tidy_line` 으로 옮김(공시 요약·시황이 같은 음슴체 규칙).

- **2026-07-27 — cron claude 인증을 장기 토큰으로 전환** (`daccada`). 배경: 이날 아침
  08시 cron 이 `Not logged in` 으로 실패. 원인은 키체인 OAuth 토큰이 며칠 만에
  만료되는데(24일 저녁 만료) 주말 스킵으로 갱신 기회가 없었고, cron 은 GUI 세션이
  아니라 갱신·키체인 접근이 막힌 것. `claude setup-token` 장기 토큰(1년)을
  `config/claude.json` 에 두고 `claude_cli.ask` 가 `CLAUDE_CODE_OAUTH_TOKEN` 으로
  주입하도록 함. 토큰 인식·실제 헤드리스 호출 성공까지 확인 완료.
  - 27일 아침 브리핑은 cron 실패 후 이날 오후 수동(`python pipeline.py daily`)으로
    게재 완료(6종목·10건, seen.json 24건). 미결 없음.
- **2026-07-24 — 주말·공휴일 제외** (`341e2af`). cron 은 `daily --skip-holiday` 로
  부르고, `common.holiday_reason()`(`holidays.SouthKorea`)이 휴일이면 조용히 종료.
- **2026-07-24 — 자동실행 launchd → cron 전환** (`91165b7`).

## 무엇이 동작하는가 (실행으로 확인)

- `highlight` → '특징주' 기사 → 종목별 이슈 3줄 + 긍정/부정 판정 + 등락률
- `market` → 간밤 미국 증시 지수 + 흐름 요약
- `theme` / `naver` / `feature` / `dart` → 이전 방식 (daily 에서는 미사용)
- `daily` → 전 구간 실행, HTML 리포트 생성·서버 저장·텔레그램 발송
- HTML 리포트: 맨 위 간밤 미국 증시(지수 카드 + 흐름 2~3줄 + 근거 기사 링크),
  특징주 칩 목록(종목명 + 등락률, 클릭 시 해당 이슈로 이동), 종목별 방향
  배지(긍정/부정/중립)·현재가·등락률·이슈 3줄·특징주 기사 링크,
  종목명 클릭 시 맨 위로 복귀
- 텔레그램 알림(성공/스킵/에러) — 이모지 없이 방향 집계·종목코드 포함
- **cron 매일 07:00 등록 완료**: `/Users/sh/blog_claude.sh` + crontab
  `0 7 * * * /Users/sh/blog_claude.sh`. 최소 환경(PATH=/usr/bin:/bin)에서 동작 확인.
  스크립트는 `daily --skip-holiday` 로 부른다 → 주말·공휴일(`holidays` 라이브러리)엔
  자동 실행이 조용히 걸러진다(수동 실행엔 영향 없음).

## 무엇이 아직인가

- **야간선물 시황**(2026-07-28 보류). 미국 증시처럼 리포트에 얹으려 했으나 **인증키
  없이 붙일 소스가 없다.** 조사 결과는 아래와 같고, 증권사 API 가 준비되면 붙인다.
  - 네이버: 코스피200 정규장 지수(`polling.finance.naver.com/api/realtime/domestic/
    index/KPI200`)는 열려 있지만 **야간선물은 제공 안 함**
  - KRX 정보데이터시스템: 주간·야간 시세 화면(`menuId=MDC0201050103`)은 있으나
    `getJsonData.cmd` 가 403/LOGOUT 으로 막히고 동적 로딩 — cron 기반으론 부적합
  - KRX OpenAPI(openapi.krx.co.kr): 공식·무료지만 인증키 발급 필요, 야간거래 포함
    여부는 발급 후 확인해야 함
  - 뉴스: 네이버 금융 뉴스 3개 섹션 × 3쪽에 "야간" 기사 0건 → 뉴스만으로는 불가
  - 참고: 야간선물은 2025-06-09 부터 CME 연계가 아니라 **KRX 자체 야간거래**
    (18:00~익일 05~06시). 조회 기준일은 야간거래 **종료일(T+1)**.
- **공시 시각**: DART 가 날짜만 제공 → 리포트에 날짜만. 분 단위가 필요하면 KIND 연동.
- 외장 SSD(`/Volumes/extSSD4tb`) 미마운트 시 저장 실패 → 텔레그램 에러 알림으로 감지.

## 주의사항

- **claude 인증**: cron 은 키체인 로그인을 못 빌리고 OAuth 토큰이 며칠 만에 만료된다.
  `claude setup-token` 으로 발급한 장기 토큰을 `config/claude.json` 의 `oauth_token`
  에 넣어둔다. 토큰이 만료되면(1년 뒤 등) 재발급 후 이 값만 갈아끼우면 된다.
- 응답은 항상 한글. 요금제는 **Pro**(Max 아님) — `highlight.top_n` 이 곧 claude
  호출 수(+미국 시황 1회)이므로 늘릴 때 유의.
- 비밀값은 `config/*.json` 에만. 에러 메시지에 토큰 싣지 않기.
- 요약 말끝은 음슴체(했음/뜻임/어려움). `common.tidy_line` 이 마침표·어미중복 정리.
- 실패 시 리포트를 올리지 않는다 — 불완전한 글보다 하루 거르는 게 낫다.
